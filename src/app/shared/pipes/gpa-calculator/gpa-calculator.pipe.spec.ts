import { GpaCalculatorPipe } from './gpa-calculator.pipe';

describe('GpaCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new GpaCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});
